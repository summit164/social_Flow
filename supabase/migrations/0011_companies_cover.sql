-- ============================================================
-- 0011 — Обложка у компании
-- ============================================================
-- Логотип уже есть (logo_url). Добавляем cover_url для баннера/фона
-- на карточке компании в стиле hh/Хабр Карьера.
-- Файлы храним в существующем bucket "avatars" по пути:
--   avatars/{owner_id}/companies/{company_id}/logo.{ext}
--   avatars/{owner_id}/companies/{company_id}/cover.{ext}
-- Это позволяет использовать уже работающие политики (первая папка = owner).
-- ============================================================

alter table public.companies
  add column cover_url text;
